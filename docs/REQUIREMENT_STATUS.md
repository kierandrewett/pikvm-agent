# Product requirement status

This ledger preserves the full scope discussed for the PiKVM harness. A green
test for one adapter or one benchmark sample does not close a broader product
requirement. `Proven` means current authoritative evidence covers the stated
scope; `Partial` and `Blocked` are not release claims.

Last audited: 2026-07-28.

| Requirement | Status | Current evidence | What remains |
| --- | --- | --- | --- |
| Never touch the production Windows laptop or production PiKVM during development | Proven for recorded harness work | Lab supervisor rejects production port `47615`; benchmark reports identify isolated targets; endpoint scan contains no supplied VNC address | Keep this as a permanent release invariant |
| Select VNC/PiKVM targets at runtime; never hard-code the supplied address | Proven | `lab up --vnc`, runtime-only environment selection, generated daemon/harness/client config endpoint tests, source scan | Repeat on the future Linux lab |
| Present a PiKVM-compatible mock API over arbitrary VNC | Proven locally | VNC transport, PiKVM-shaped adapter, three-process visible lab lifecycle and contract tests | Package and exercise against more VNC servers |
| Prevent two local labs from controlling the same VNC target | Proven locally and against live disposable-target contention | The first live managed-Windows diagnostic exposed process-local client accounting. The adapter now acquires a canonical-endpoint OS lease before RFB, stores only a versioned endpoint digest, refuses an equivalent second endpoint spelling before connect, and releases after shutdown or failed connection. A fresh deliberately competing adapter was refused in 1.550 seconds before RFB with zero input sent. Its console showed the concise lease cause without a traceback; the full redacted trace remained in a mode-0600 report containing no runtime endpoint. Forty focused contracts and the 1,199-test full Python suite pass with one intentional skip | Repeat on the future Linux lab and add coordination across separate edge hosts where an OS-local lease is insufficient |
| Windows helper reports exact text in its own benchmark editor, raw input traces elsewhere, focus/desktop identity, and selected file bytes | Proven as a scoped lab oracle | Observer service, receiver, bootstrap, visual-oracle and Windows benchmark artifacts; low-level hooks report VK/scan events rather than pretending to reconstruct layout/IME-dependent prose in arbitrary apps | Installer/signing and repeated clean-VM deployment; Office exactness comes from saved-file bytes and semantic artifact checks |
| Validate long prose, code, special characters, duplicate suffixes, and file contents | Partial | Deterministic Windows trials reached 581/581 prose and 142/142 code; file SHA evidence exists. Line-break boundaries consume adjacent horizontal whitespace into exactly one non-submitting space while preserving deliberate inline spacing. A deterministic repro found a second cause of doubled spaces: a chunk beginning with one space could partially land while stale video/OCR still matched the pre-chunk prefix, causing the non-idempotent chunk to be replayed. Typing is now at-most-once and ambiguous delivery stops unverified. A seeded 1,000-case stale-readback regression emitted every canonical payload exactly once with no introduced doubled spaces. Receipts distinguish requested, delivery, logical issued-prefix, actual emitted stream, complete OCR, and evaluated-frame SHA-256 values. Exact OCR retains duplicate suffixes and surrounding text instead of carving out an intended substring; an intent-matching alternative can request a recheck but cannot authorize completion. Whitespace, case-plus-spacing, incomplete, and precise-contains results all stop before active follow-ups | Repeat the at-most-once path on the disposable VM when its lease is free; the complete physical loop and generic OCR remain below release quality |
| Give a live model a routine Office task and prove the saved result | Partial; one clean Excel pass and one recovered Word artifact | The disposable Windows VM now has a failure-inclusive 1/5 Excel acceptance result. The final live managed run used Opus for reasoning/acting and Haiku for checking, completed 23/25 attempted actions, saved a 9,437-byte XLSX, and passed 29/29 independent semantic checks after reopening it. A separate Word run produced a 16,081-byte DOCX that passed 11/11 semantic checks only after bounded post-run observer recovery, so it is not a clean end-to-end pass. The current UI replay projects all 25 durable Excel action attempts into one compact transcript with one user message, one assistant message, zero branch counters, and zero duplicate planning rows | Repeat Excel across fresh filenames and clean VMs, obtain a clean end-to-end Word pass without recovery, reduce the 29-minute latency, and expand beyond one workbook/document |
| Avoid long base64/script HID hacks | Partial | Bounded typing, ≤16-character visually guarded printer submissions, chunk verification, no whole-draft recovery replay, visual oracle, short bootstrap commands, and a seeded 1,000-payload pre-HID corpus that refused 800/800 command-shaped dense Base64, encoded PowerShell, heredoc, and long nested-shell cases—including contiguous-action segmentation—with 0/200 safe false positives | Add an explicit byte-verified transfer channel; long realistic edits still need repeated helper-free visual proof |
| Move large artifacts through an explicit exact-byte channel | Partial; target-free transaction only | The mode-0600 builder and manifest prove extracted bytes and enforce safe names/budgets/no-overwrite; 19/19 target-free contracts cover exact browser approval, agent/wrong-ID refusal, model-surface exclusion, durable upload/select/attach/detach/remove states, definite rollback, ambiguous cleanup latching without retry, identity/lease/stop handling, private storage, UI projection, session-bound daemon-adapter shape, and explicit unsupported VNC. No model-facing tool or daemon mutation route exists | Add a one-time daemon capability bound to the approved checkpoint, PiKVM backend/HTTP contracts, prerequisite packaging, then isolated Windows and Linux physical trials |
| Provide 1,000 randomized blind OCR examples | Proven for corpus generation | Seeded unique 1,000-case corpus and blind-provider contract tests | None for corpus construction |
| Make OCR reliable enough for production verification | Failing release gate; doubled-space false verification removed | The runtime and example config use the benchmarked PSM 6, 2× raw/preprocessed syntax-aware ensemble; general selected exact remains 56.9% and PaddleOCR 78.9%. A new seeded, opaque 1,000-field spacing corpus balances 500 clean fields against 500 fields containing one injected doubled space across short/long and proportional/monospace text. The unsafe baseline trusted canonical OCR text, detected only 164/500 corruptions, and falsely verified 181/500 because TSV text collapses visible gaps. The repaired exact path requires independently repeated multi-scale spacing geometry before canonical text can authorize completion; uncalibrated spacing is an explicit uncertainty, not a match. Its ten validated shards merged to exactly 1,000 unique cases: zero doubled spaces were falsely verified, 295/500 were identified as spacing mismatches, three clean fields raised conservative spacing alarms, and 555 cases abstained. Only 147/500 clean fields auto-verified, so this is a safety improvement rather than a production-quality OCR claim. Exact field hashes remain hashes of OCR evidence, not guest bytes | Improve short-field and proportional-font gap evidence without reintroducing false verification; run the same frozen corpus through hybrid/local and fast multimodal providers; live-test the complete-field crop; use observer/app/file hashes where exact bytes are available |
| Intercept ordinary Claude/Codex/OpenCode raw MCP calls | Proven locally | Actual MCP `ClientSession` dispatch test; preflight occurs before tool body; public startup and dispatch now refuse missing visibility | Live probes in each supported coding client release |
| Make the dedicated harness—not the coding CLI—the default operator loop | Partial: first-party runtime and path-free client boundary proven; ambient client cutover remains | Source re-audit proved the referenced Claude session made 551/551 calls directly, the seven audited Codex histories made 1,482/1,482 low-level calls, and the two OpenCode histories made 95/95 direct calls; none used the current managed `computer_*` facade. Four older Claude conversations made 37 calls through a different legacy autonomous route whose internal model was not recorded. Shipped configs expose only `managed-mcp`; 21 submit-once tests prove the harness crosses normal action slices and safe replans without client Continue calls. Codex completed in 13.700 seconds and Claude Code 2.1.220 completed in 16.944 seconds, each using only start/status and producing 22 durable events, three deterministic reasoner/controller/verifier calls, one action checkpoint, and one visual-verification revision. OpenCode 1.14.44 retained two preflight failures, then completed in 20.155 seconds after the launcher resolved its HOME-relative wrapper before creating isolated config/cache/state/data roots. Its final pure-mode run used only `pikvm_computer_start_task` and `pikvm_computer_status`; the outer model remained explicitly unreported because the JSON stream did not attest it. Caller label, exact inner MCP action, action model, and verification state were visible together in the first-party chat UI for Claude and OpenCode. A separate chat-originated live Codex inner loop also passed, but took 336.916 seconds for one bounded click. Gemini 0.35.3 passed the native effective-settings/admin-policy audit and registered exactly one managed MCP with extensions, skills, hooks, context, permanent approvals, and shared runtime roots disabled. Its linked client-owned OAuth was then rejected by the external service as an unsupported client before any model response, tool call, or harness run. The desktop now publishes and revokes a reduced agent-only active runtime; `active-managed-mcp` reads its stable per-user location, and `active-client-config` emits path-free registrations for all four clients with no machine endpoint, harness/runtime path, or token name/value. A 154-test focused slice covers the four native config shapes, hostile argument rejection, runtime ownership, and real stdio inventories containing exactly five `computer_*` tools. The current ambient audit remains deliberately failing: Codex 0.144.4 and OpenCode 1.14.44 resolve raw PiKVM, Gemini 0.35.3 has no managed registration, and Claude 2.1.220 is managed only in this explicit project scope. No client config was rewritten and no target was contacted | Add a transactional reviewed client installer/rollback, cut over supported ambient clients only after the new desktop build is running, obtain a supported Gemini route, and repeat on the disposable Windows/Office target |
| Behave as a normal chat/research agent without acquiring the computer | Partial; live Claude and Codex target-free routes pass | Durable multi-turn conversations, ordinary replies, one-at-a-time visible MCP calls, exact tool approval, stable streamed message identity, and paused/error projection are covered by focused backend/frontend tests. General capabilities and the assistant-to-computer transition retain the exact selecting provider/model/latency; the latter renders as a visible `computer_start_task` call with the exact handed-off task even when the model supplied no prose. A first-class, mode-0600 live-provider acceptance command has no daemon or computer transport. Claude OAuth passed the current 5/5 contract in 42.804s: greeting 9.303s, general question 5.437s, sourced research through visible `web.search_text` 15.907s, computer hand-off 5.188s, and a consequential send-message approval canary 6.970s. The canary reached `needs_approval` with the exact request held locally and zero broker executions; it has no external messaging transport. Codex OAuth initially failed because its strict output-schema transport rejected the assistant's open-ended argument object; the retained 0/4 diagnostic led to a closed `arguments_json` wire contract. The corrected Codex full run passed 4/4 in 549.453s and remained unsuitable as the default interactive route. Codex then passed only the new approval canary 1/1 in 104.254s with zero executions. Every case emitted visible activity within 1–2ms, research cited `www.python.org`, and hand-off sinks attested `computer_target_contacted: false`. The production server can now start with no daemon, agent token, observer token, direct-call coordinator, or computer adapter. A fresh isolated Electron/CDP pass drove the real first-party chat UI through Claude OAuth with no computer adapter: 3/3 cases passed across exact chat, official-source research, and ordinary knowledge; five live provider calls completed without error; `web.search_text` and `web.extract_content` remained named in the collapsed transcript; the fully sampled case showed progress within 136 ms with one assistant reply, at most one progress indicator, zero branch counters, no session, and zero computer events. A target-free synthetic browser audit also showed the attributed hand-off as one compact completed tool row and all four fixture tasks in the task rail without contacting a computer. `harness check` reports chat and computer readiness independently, while `--require-computer` restores a strict full-control gate | Run the identical full contract against a valid API credential route and record browser-visible approval and computer hand-off traces |
| Preserve guarded direct mode for existing client-driven workflows | Proven locally | Explicit `--control-mode direct`, direct-call UI label, exact redacted arguments plus durable outcome/latency, internal path/raw-payload exclusion, pause/stop/approval contracts, required world version/control epoch, and daemon-enforced non-blank caller-stable idempotency keys; degraded observe mode permits only perception and emergency-stop tools when visibility is down | Live client matrix across Claude, Codex, Gemini, and OpenCode |
| Never imply direct mode has an independent verifier | Proven in UI contract | Persistent managed/direct assurance banner plus Chromium, Firefox, and WebKit audit: `Guarded direct`, actual caller/provider/model visible, managed assurance and composer absent | Repeat against a live direct computer session |
| Show the live screen, exact current tool call, model/provider, freshness, policy, verification, and approvals together | Partial | The assistant-ui chat workspace keeps a completed computer action to one quiet row: exact input, full MCP tool name, elapsed time, action-selecting model, and verification state. Opening it puts authenticated before/after evidence first. Typing shows requested and canonical delivery lengths, issued progress, actual emitted-payload integrity, complete OCR read-back, corrections, and one compact payload/OCR/frame fingerprint. `exact_visual_readback` binds the OCR result to the SHA-256 of the exact captured frame it evaluated; it is still screen evidence, never a guest acknowledgement. Helper/app/file bytes remain the lab oracle. Ordinary MCP results now show a concise outcome and bounded argument summary in the transcript while preserving exact arguments and raw output behind `Details`; the selecting provider/model remains on the same action. Managed outer-client provenance remains visible in the header, while guarded-direct mode shows the actual outer caller/provider/model and removes managed assurance and the composer. Mixed managed `computer_start_task` plus `pikvm_*` turns now stay inside one computer-specific timeline rather than falling back to a generic tool chain; its count reflects exact low-level inputs, it opens during live input, and its current row names the semantic sequence. The authenticated 1,200-event/12-action contract passes Chromium 149, Firefox 151, and WebKit 26.5. A later isolated Electron/CDP fixture rendered one expanded 64-input computer group, the current click, and zero generic managed-computer chains. The preserved live Excel run adds a 532-event real-computer replay: the API retained a bounded 500-event diagnostic tail plus all 152 user-visible timeline events, and Electron reported all 25 durable actions in one tool group with one assistant message, zero branch counters, and zero duplicate planning rows | Complete another contemporaneous live-action UI audit after this projection fix, obtain a clean Word acceptance, and run multi-hour sustained-stream/decode chaos |
| Keep the UI responsive and light | Partial | No remote assets; the integrated production assistant-ui/shadcn workspace stays below its checked asset caps. Computer-use disclosures use one narrow, flattened transcript instead of nested card walls, keep status text visible, bound exact-input/read-back/raw-argument regions, and disable state motion under reduced-motion preferences. Generic tool calls avoid dumping raw JSON into the primary transcript; all three audited engines retained exact evidence behind a bounded disclosure with zero overflow. The selected run uses one authenticated SSE connection, a bounded 500-event diagnostic tail, a filtered 5,000-event user-visible timeline ceiling, and coalesced live previews with a 4 MiB response cap, eight-session/32 MiB cache, and 450 ms minimum upstream interval. The current production build is 244,805 gzip-9 JavaScript bytes and 18,252 gzip-9 CSS bytes. Chromium, Firefox, and WebKit each rendered the 12-action/20-preview fixture at 1440×900 and 390×844 with zero document, conversation, or action overflow in 3.796s, 8.067s, and 6.857s respectively. The isolated 25-action live-run replay navigated in 126.4 ms and retained 10.35 MiB JavaScript heap after an explicit CDP garbage collection | Measure browser decode time and resident memory under continuous live frames, real-filesystem SQLite, and multi-hour browser/process behavior |
| Make consequential actions genuinely human-approved | Proven for local default paths and live OAuth canary | No model-facing approval tools; distinct operator/agent/observer credentials; browser origin plus exact intent; daemon policy hold; every direct/debug action-bearing MCP tool conservatively advertises `destructiveHint=true`; managed task submission stays interruption-based rather than prompting for every safe task; Ctrl+Enter and Outlook-style Alt+S send shortcuts are held; infrastructure auto-approval flags, Terraform/OpenTofu apply or destroy, and forced OCI deletion/termination require human approval from command text alone on direct and managed paths. A target-free simulated send-message tool was requested by live Claude and Codex OAuth models, reached `needs_approval` in both runs, and recorded zero broker executions; the canary has no external transport. The generic tool UI requires Allow once followed by a second exact confirmation, while Deny remains immediate. The seeded 1,000-label OCR-noise permission corpus catches 800/800 dangerous controls with 0/200 safe false positives | Two-person approval, signed short-lived capabilities, hosted identity, real email/Teams and physical-app replay |
| Keep emergency stop independent of model and web UI | Proven against isolated daemon | Out-of-band CLI audit halted active HID and confirmed quiescence in 0.34 seconds | Physical-device drills and edge-daemon-loss runbook |
| Support OAuth-backed providers without copying cached tokens | Partial | Codex CLI, Claude Code, and OpenCode own their login stores and have completed authenticated target-free managed tasks without copying credentials into harness state. Codex and OpenCode link only their client-owned OAuth files into otherwise isolated writable state; Claude preserves its owned login/keychain path while refusing ambient `ANTHROPIC_API_KEY` and unrelated secrets. Gemini's disposable dedicated profile linked rather than copied the CLI-owned OAuth files, passed native policy enforcement, and registered one managed MCP, but the external individual-tier service rejected Gemini CLI 0.35.3 before model work. Its runtime now isolates home and XDG caches as well as workspace/settings. Azure OpenAI and Vertex AI support isolated exact-argv credential commands with empty stdin and allow-listed environment inheritance. `harness init` writes only command/profile environment names; `docs/PROVIDER_SUPPORT.md` defines canonical tiers, credential ownership, compatibility evidence, version policy, and promotion criteria. The authenticated Models sheet derives from the same ten-adapter catalog and reports credential ownership without a browser secret-entry form or credential source paths | Obtain a supported Gemini OAuth path; live Azure/Vertex compatibility; more client versions and clean-install probes; add a reviewed first-party account-connection flow |
| Support broad API providers | Partial | Native OpenAI Responses, Azure OpenAI Responses with API-key/Entra modes, OpenAI-compatible Chat Completions, Anthropic Messages, Gemini AI Studio and Vertex AI generateContent, and generic subprocess adapters; secret-free onboarding composes ordered routes; durable provider-attempt cap and opt-in explicit-price reservation/settlement are enforced before HID. The chat-side Models sheet progressively exposes all ten stable/beta/bridge adapters, image/schema interface, configured route position, readiness, coarse results, and conformance state. The first native Anthropic Messages live canary reached the provider with `claude-sonnet-5` but the environment-owned credential was rejected as `authentication-failed`; the retained 0/1 report records one provider call, zero tool requests/calls, zero consequential executions, and no computer contact | Obtain a valid API credential route, add provider-side hard caps, validate real customer price tables, and complete a secure account-connection flow |
| Compare OAuth/API providers on identical vision and schema work | Partial; live OAuth matrix n=3/provider, API attempt failing auth | The target-free seeded 960×540 suite uses one strict schema, explicit provider-call consent, a failure-inclusive denominator, normalized usage, and a mode-0600 no-overwrite report. Its scheduler now allows cross-provider concurrency while limiting each provider to one in-flight case. The current frozen Codex account-default versus Claude Opus run scored 6/6 exact and schema-valid with zero failures: Claude 15.693s median / 16.461s p95; Codex 105.739s / 114.879s. The first Anthropic API assistant canary is retained as a credential failure rather than dropped from evidence; it did not reach a schema or behavior verdict | Expand to at least 100 cases per route and add valid OpenAI, Anthropic, Gemini, Azure, Vertex, and compatible API credentials before making a general provider ranking |
| Use a fast controller plus stronger reasoner/verifier with fallback | Partial; OAuth fallback selected, fast model absent | Independent role routes, pre-HID fallback, cooldown, repair, safe-draft downgrade, and durable per-role attribution pass. The all-Codex inner loop spent 336.753 of 336.916 seconds in model calls for one 1ms click. On identical blind screen/schema work, both OAuth adapters were 3/3 exact, but Claude Opus was 6.7× faster than Codex account-default at 15.693s versus 105.739s median. Two-CLI onboarding and lab configs use Claude then Codex for acting/checking fallback, while configured API/gateway routes remain ahead of both. A paired Calculator diagnostic retained both outcomes: default Claude CLI effort completed with one controller and one verifier call in 118.195s; forcing `low` for Haiku controller/verifier took 251.432s, three calls per role, and three HID bursts for the same eight inputs. The CLI effort is explicitly configurable but is not forced in generated defaults. The Models sheet warns that an accurate acting primary above five seconds is not a fast-path model. The benchmark does not rank general reasoning quality, and neither measured OAuth route qualifies as the desired fast controller | Run the same frozen suite plus action-selection cases against a sub-5s API model, then publish and validate a fast-controller/strong-reasoner/verifier route in the disposable Windows and Linux labs |
| Attribute the actual model used | Partial | API responses record resolved model fields; CLI adapters retain reported model/alias; direct mode accepts explicit caller metadata and labels it launcher-declared | MCP clients do not standardize model identity; unverifiable labels must remain marked as declared |
| Expose speed, accuracy, efficiency, failures, and denominators publicly | Proven for current recorded evidence | Central `bench/README.md`, durable JSON reports, run-metrics command, failure-inclusive denominators, and a manifest-driven headline whose rendered values plus evidence digests are enforced by `harness scorecard --check` and pytest | Add every new public suite to the checked manifest |
| Deeply audit prior PiKVM conversations and corrections | Proven for available local histories; remediation remains incomplete | 24 conversations, 4,453 calls, 70 incidents, corrected call-level model attribution and corrective-action reconstruction; the source pass fixed a false silent-transport-loss claim, replaced an over-broad rejected-action narrative with a confirmed completed forced termination, and added the missing Gemini 3.6 dry-run/auto-approval incident; a checked ledger maps all 47 critical/high incidents exactly once: 7 locally covered, 40 partial, 0 open; grounded Replace/Replace All/Replace in files clicks and direct Undo/Redo require approval; a helper-backed editor evaluator now captures exact baselines, unique logical-line bounds, expected diffs, diagnostic invariants, a one-cycle repair budget, and exact rollback payloads | Wire the editor evaluator into managed transactions and prove it in real Word, VS Code, and rich-text trials; advance all 40 partial mappings with real-app evidence; re-run when new history sources are added |
| ScreenSpot-Pro live-model evaluation | Partial | Codex 73/100 seeded sample; Claude 5/5 diagnostic sample | Full pinned 1,581-case runs and a statistically useful Claude sample |
| OSWorld live-model evaluation through harness, MCP, daemon, and official evaluator | Partial | Nine-task current set at 6/9; all-attempt denominator 6/33 plus 11 unscored failures; operator console integrated | Larger frozen-code run and another live iteration |
| Windows Agent Arena evaluation | Blocked by missing external asset | 154 official tasks discovered and pinned | Terms-gated official golden image/ISO is not available |
| Linux real-machine/VNC validation | Missing | Transport is OS-neutral by design | Run the same observer-free and helper-assisted suite on a Linux VM |
| Sellable local operator edition | Partial | Core harness, UI, provider config, transcript audit, VNC lab, benchmark reporting, Office artifact acceptance, target-free managed smoke lab, a mode-0600 offline support bundle, and a wheel inspector that verifies `RECORD`, CLI wiring, budget/Office/smoke modules, UI assets, size/path limits, and secret/runtime exclusions exist. A prior offline Hatch wheel installed into a fresh venv and loaded wheel code against the existing dependency layer; the new smoke-lab module is now required and that artifact is stale | Rebuild and reinspect the updated wheel; complete a dependency-isolated clean-venv install where locked compiled wheels are available; add signing, installer, and updater |
| Team edge edition | Missing | Product architecture is documented | Local identities/roles, machine registry, two-person approval, audit export |
| Hosted SaaS control plane | Missing | Threat boundary and release sequence are documented | Tenant isolation, edge enrollment/mTLS, relay, retention, SSO, billing, pen test |

### 2026-07-28 managed-default increment

The desktop's stable MCP launcher now chooses the managed five-tool facade by
default. Its owner-only runtime carries the agent capability required by that
facade but still excludes the browser operator token and daemon approval
capability. Guarded direct control remains available only through the explicit
`--control-mode direct` compatibility argument.

An isolated Electron-owned harness completed a real stdio MCP handshake through
the built desktop launcher. The inventory contained exactly the five
`computer_*` controls and zero raw `pikvm_*` tools. A separate three-second
provider hold reached the browser-visible “Waiting for a response” stage 20 ms
after run creation, completed as one assistant reply, and created no computer
session or input event. The completed DOM had no branch counter, duplicate
planning row, Terminal control, or horizontal overflow. No screenshot or
remote target was used. Evidence:
[`desktop-managed-default-and-progress.json`](../bench/results/2026-07-28/ui/desktop-managed-default-and-progress.json).

The follow-on active-runtime increment removes disposable paths from persistent
client registrations. A healthy desktop now publishes a reduced, owner-only
agent runtime at the platform's stable per-user runtime location and revokes
only the generation it owns. `active-managed-mcp` consumes that handoff;
`active-client-config` emits Codex, Claude, Gemini, and OpenCode registrations
without a harness path, runtime path, machine endpoint, token environment name,
or token value. The agent and desktop focused suites pass 154/154 and 23/23
respectively, and the desktop production build succeeds.

This is not yet an ambient-client cutover claim. The same-day effective route
audit found raw PiKVM registrations in Codex and OpenCode, no managed
registration in Gemini, and an ambiguous Claude user registration. This
repository's explicit Claude project scope resolves safely to exactly one
managed registration. Nothing was rewritten, no installed desktop was
restarted, and no computer target was contacted. Evidence:
[`effective-client-route-audit.json`](../bench/results/2026-07-28/safety/effective-client-route-audit.json).

## Current verification boundary

The completed post-change selection is recorded in
[`../bench/README.md`](../bench/README.md). This restricted runner still hangs
in several static/screenshot/temp-file ASGI tests and cannot start the required
Docker/VM path for the next OSWorld iteration. A minimal probe also proves that
`asyncio.to_thread` cannot return reliably in this runner, so remaining
threaded vision cases are recorded as not run rather than inferred passes.
PaddleOCR no longer uses that seam: its native worker is process-isolated and
passed a bounded five-case lifecycle diagnostic, but the full hybrid accuracy
run remains outstanding.

The product is therefore **not complete**. The next release-critical work is:

1. make the Windows seven-trial loop pass repeatedly;
2. execute the full hybrid OCR and live field-crop passes, then raise ambiguous
   numeric/punctuation exactness;
3. run a frozen-code, larger public benchmark matrix;
4. obtain a supported Gemini OAuth/API path, repeat the managed Codex, Claude,
   and OpenCode clients on real tasks, and complete multi-hour UI streaming;
5. build the signed local operator package before starting hosted control-plane
   work.
